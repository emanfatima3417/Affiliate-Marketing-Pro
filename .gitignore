# Safety net at the repo root - backend/ and frontend/ each have their own
# more specific .gitignore too, but this catches anything at the top level.
node_modules
.next
.env
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
